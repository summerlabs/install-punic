name: test

on: 
  push:
    branches:
      - master

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: summerlabs/install-punic
      - name: check punic 
        run: punic --version



